const DB = require('./../infra/db')
const uuidv4 = require('uuid/v4')

/* I don't think this file will ever be used. Or should model handle filesystem?
   getFolderTree in service layer should be moved here then.
*/

const FolderModel = {
}

module.exports = FolderModel